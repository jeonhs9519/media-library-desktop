# Next Task

Last updated: 2026-05-06

이 문서는 다음 작업 세션에서 바로 이어받기 좋은 우선순위만 남깁니다. 완료된 작업의 상태 기록은 `current-status.md`, 변경 이력은 `changelog.md`, 장기 후보는 `backlog.md`에서 관리합니다.

## 우선순위

1. 테스트 보강
2. 문서와 구조 안정화
3. Tauri 재구축 준비 문서화

## 바로 시작하기 좋은 작업

### 1. 테스트 보강

- 현재 E2E는 프로필 선택 화면 도입 전 기준이므로, 프로필 선택/자동 진입 흐름을 반영해 갱신합니다.
- `items`와 프로필 이관 흐름에 대한 단위 테스트 또는 통합 테스트를 추가합니다.
- 최소한 다음 흐름은 자동 검증 대상으로 잡습니다.
- 파일 추가
- 중복 파일 차단
- bulk relink 충돌 처리
- `.hdt` preview/apply
- 프로필 선택, 생성, 이름 변경, 전환, 삭제
- 항목 이동/복사, 중복 대상 비활성화, 태그 이름 기준 재연결
- 프로필 삭제 이관/덮어쓰기, 대상 프로필 중복 항목 삭제, 플레이리스트 itemId 대체
- 라이브러리 목록 roving focus와 상세 팝업 focus return
- 컨텍스트 메뉴의 출처 URL 열기 활성/비활성 조건
- 플레이리스트 추가, 재정렬, 뷰어 자동 다음 항목 실행 회귀 테스트

### 2. 문서와 구조 안정화

- `architecture.md`가 현재 프로필 구조와 IPC를 따라가도록 유지합니다.
- `LibraryPage.tsx`, `ItemDetailPage.tsx`, `App.tsx`는 기능이 많아졌으므로, 다음 큰 기능 전에 컴포넌트와 hook 경계가 충분한지 점검합니다.
- i18n 키, toast, Modal 사용 패턴은 새 UI 추가 시 같은 패턴을 재사용합니다.

### 3. Tauri 재구축 준비

- 현재 Electron 앱을 기준 구현체로 보고, 기능 목록, 화면 목록, DB 스키마, IPC/API, 파일 처리 흐름을 문서화합니다.
- 재구축 전에 테스트 시나리오와 소스 구조 분석 문서를 먼저 작성합니다.

## 운영 기준

- TODO성 아이템은 `backlog.md`로 누적합니다.
- 실제 착수 항목만 이 문서로 승격합니다.
- 완료된 작업은 이 문서에서 제거하고 `current-status.md` 또는 `changelog.md`에 흡수합니다.

## 재구축 전제 조건

- Tauri 재구축은 현재 Electron 앱의 핵심 기능 안정화와 테스트 보강 이후 진행합니다.
- 재구축 전에 테스트 시나리오, 소스 분석 문서, 재구축 계획서를 먼저 작성합니다.
