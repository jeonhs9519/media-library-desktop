# Next Task

Last updated: 2026-04-29

이 문서는 다음 작업 세션에서 바로 이어받기 좋은 우선순위만 남깁니다.

## 우선순위

1. 라이브러리 화면 분리 작업 착수
2. 태그 기반 필터링 기능 설계 및 구현
3. 핵심 사용자 흐름 테스트 보강
4. 플레이리스트 기능 범위 확정

## 바로 시작하기 좋은 작업

### 1. 라이브러리 화면 분리

- `LibraryPage.tsx`에서 다음 책임을 분리합니다.
- 검색/필터 상태
- 파일 추가 및 `.hdt` 가져오기 모달
- 설정 모달과 bulk relink UI
- 카드 렌더링과 목록 렌더링

기대 효과:
- 다음 기능 추가 시 변경 범위를 줄일 수 있습니다.
- 테스트 가능한 단위가 생깁니다.

### 2. 태그 필터링 기능 추가

- 라이브러리 상단에 태그 목록을 노출합니다.
- 태그 선택 시 AND 조건 필터링으로 동작하게 합니다.
- 태그별 건수 계산과 정렬 정책을 정합니다.
- `태그 미지정` 처리 방식과 다국어 키를 함께 정의합니다.

### 3. 테스트 보강

- `items` 흐름에 대한 단위 테스트 또는 통합 테스트를 추가합니다.
- 최소한 다음 흐름은 자동 검증 대상으로 잡는 것이 좋습니다.
- 파일 추가
- 중복 파일 차단
- bulk relink 충돌 처리
- `.hdt` preview/apply

### 4. 플레이리스트 범위 정리

- 지원 대상: `book`, `comic`, `video`
- 비지원 대상(`other`) 차단 UX 정의
- 자동 다음 항목 재생/열람 트리거 조건 정리

## 이번 문서 정리 이후 권장 후속 작업

- `docs/current-status.md`를 기준으로 큰 구조 변경 시 상태 문서를 갱신
- TODO성 아이템은 `docs/backlog.md`로만 누적
- 실제 착수 항목만 이 문서로 승격
